apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-node2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-node2
  template:
    metadata:
      labels:
        app: worker-node2
    spec:
      nodeSelector:
        kubernetes.io/hostname: node2-worker
      containers:
        - name: worker
          image: williamsdock/mini-dc-worker:v7
          env:
            - name: REDIS_HOST
              value: "redis"
            - name: REDIS_PORT_NUM
              value: "6379"
            - name: QUEUE_NAME
              value: "jobs"
            - name: WORKER_NAME
              value: "node2-worker"
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: LOG_FILE
              value: "/data/job_results.csv"
          volumeMounts:
            - name: job-logs
              mountPath: /data
      volumes:
        - name: job-logs
          hostPath:
            path: /tmp/mini-dc-logs
            type: DirectoryOrCreate
